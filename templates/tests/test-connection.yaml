apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "educacionit-app.fullname" . }}-test-connection"
  labels:
    {{- include "educacionit-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "educacionit-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
